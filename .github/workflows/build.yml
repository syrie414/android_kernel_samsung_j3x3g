name: Build Linkit2me Kernel (J320H)

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:20.04
      options: --user root

    steps:
      - name: Checkout Kernel Source
        uses: actions/checkout@v4

      - name: Setup Environment
        run: |
          apt-get update
          dpkg --add-architecture i386
          apt-get install -y git curl build-essential bc bison flex \
            libssl-dev libelf-dev zip gcc-multilib g++-multilib \
            libc6-dev-i386 lib32ncurses5-dev lib32z1-dev \
            libncurses5 libncurses5-dev python3 cpio
          ln -sf /usr/bin/python3 /usr/bin/python

      - name: Setup Toolchain
        run: |
          git clone https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 \
            -b android-wear-5.1.0_r1 --depth=1 toolchain
          chmod -R +x toolchain/bin/

      - name: Fix Legacy Scripts
        run: |
          sed -i 's/^YYLTYPE yylloc;/extern YYLTYPE yylloc;/' scripts/dtc/dtc-lexer.lex.c_shipped || true
          sed -i 's/^YYLTYPE yylloc;/extern YYLTYPE yylloc;/' scripts/dtc/dtc-lexer.l || true

      - name: Build Kernel (Force Console & Fix Offsets)
        run: |
          export ARCH=arm
          export CROSS_COMPILE=$(pwd)/toolchain/bin/arm-linux-androideabi-
          
          make j3x3g-dt_defconfig
          
          # 1. إجبار الكيرنل على تجاهل أوامر البوت لودر (CMDLINE)
          # هذا السطر سيكسر قيود سامسونج التي تفرض console=null
          sed -i 's/CONFIG_CMDLINE_BOOL=n/CONFIG_CMDLINE_BOOL=y/g' .config
          sed -i 's/CONFIG_CMDLINE_FORCE=n/CONFIG_CMDLINE_FORCE=y/g' .config
          sed -i 's|CONFIG_CMDLINE=.*|CONFIG_CMDLINE="console=tty0 earlyprintk=tty0,115200 androidboot.selinux=permissive"|g' .config

          # 2. تعديل عناوين الذاكرة لتناسب معالج Spreadtrum SC8830
          echo "CONFIG_MEMORY_START=0x80000000" >> .config
          echo "CONFIG_PHYS_OFFSET=0x80000000" >> .config
          
          # 3. تفعيل الـ Low Level Debug
          echo "CONFIG_DEBUG_LL=y" >> .config
          echo "CONFIG_DEBUG_SPRD_UART=y" >> .config
          echo "CONFIG_EARLY_PRINTK=y" >> .config

          # الرد التلقائي على أي أسئلة جديدة من الكومبايلر
          yes "" | make oldconfig
          
          # 4. بناء الكيرنل والـ DTB
          make -j$(nproc) zImage dtbs

      - name: Package with Appended DTB
        run: |
          git clone https://github.com/osm0sis/AnyKernel3 --depth=1 AnyKernel
          
          # إعداد السكريبت ومسار البارتشن الصحيح لـ J320H (mmcblk0p20)
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel/anykernel.sh
          sed -i 's|kernel.string=.*|kernel.string=Linkit2me-J320H-Final;|g' AnyKernel/anykernel.sh
          sed -i 's|block=/dev/block/.*|block=/dev/block/mmcblk0p20;|g' AnyKernel/anykernel.sh
          
          # دمج الكيرنل مع كل ملفات الـ DTB الناتجة (Append)
          cat arch/arm/boot/zImage arch/arm/boot/dts/*.dtb > AnyKernel/zImage
          
          # إضافة توقيع سامسونج الإجباري للإقلاع
          echo -n "SEANDROIDENFORCE" >> AnyKernel/zImage
          
          # حذف أي ملف dtb منفصل لضمان استخدام المدمج
          rm -f AnyKernel/dtb
          
          # الضغط النهائي
          cd AnyKernel
          zip -r9 "../Linkit2me-J320H-Flashable.zip" . -x ".git/*" "README.md"

      - name: Upload Final Flashable Zip
        uses: actions/upload-artifact@v4
        with:
          name: Linkit2me_J320H_Kernel
          path: Linkit2me-J320H-Flashable.zip
